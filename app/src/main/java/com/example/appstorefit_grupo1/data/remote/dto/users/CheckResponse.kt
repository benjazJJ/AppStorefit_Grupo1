package com.example.appstorefit_grupo1.data.remote.dto.users

// Respuesta genérica de los endpoints de check
data class CheckResponse(
    val available: Boolean    // true si está disponible / permitido
)
